import React from "react";
import { Layout, ComingSoon } from "../components/";

const AboutPage = () => {
    return (
        <Layout>
            <ComingSoon />
        </Layout>
    );
};

export default AboutPage;
