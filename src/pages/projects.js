import React from "react";

import { Layout } from "../components/";
import { ProjectSection } from "../components";

const ProjectsPage = () => {
    return (
        <Layout pageTitle="Projects">
            <ProjectSection paddingTop={44} />
        </Layout>
    );
};

export default ProjectsPage;
