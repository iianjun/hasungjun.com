import React from "react";

import { Layout, ComingSoon } from "../components/";
const ProjectsPage = () => {
    return (
        <Layout>
            <ComingSoon />
        </Layout>
    );
};

export default ProjectsPage;
